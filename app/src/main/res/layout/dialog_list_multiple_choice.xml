<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/rounded_all_white_smaller_radius_background"
    android:paddingHorizontal="32dp"
    android:paddingVertical="16dp"
    android:orientation="vertical"
    android:id="@+id/dialog_list_multiple_choice">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="24dp"
        android:textColor="@color/grey"
        android:fontFamily="sans-serif-medium"
        android:text="Title"
        android:id="@+id/dialog_list_multiple_choice_title"/>

    <ListView
        android:layout_width="256dp"
        android:layout_height="256dp"
        android:layout_marginTop="16dp"
        android:listSelector="@android:color/transparent"
        android:dividerHeight="4dp"
        android:divider="@android:color/transparent"
        android:id="@+id/dialog_list_multiple_choice_list"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:textAlignment="center"
        android:fontFamily="sans-serif-medium"
        android:textSize="20dp"
        android:textColor="@color/blue"
        android:text="Ok"
        android:id="@+id/dialog_list_multiple_choice_btn"/>

</LinearLayout>